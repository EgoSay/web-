<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cjw.mapper.UserMapper">
    <sql id="UserSelector">
        select * from user;
    </sql>
    <!--
    #{} 相当于占位符？，预编译，可以防SQL注入
    ${} 相当于字符串拼接，不是预编译的，不能防SQL注入
    -->
    <select id="queryUserByUserId" parameterType="int" resultType="User">
        select * from user where id = #{id}
    </select>

    <select id="findUserAsLike" parameterType="string" resultType="User">
        <!--select * from user where username like '%${value}%'-->
       select * from user where username like "%"#{v}
    </select>

    <select id="findUserByQueryVo" parameterType="com.cjw.vo.QueryVo" resultType="com.cjw.entity.User">
        select * from user where username like '%${user.username}%'
    </select>

    <!-- 根据条件查询用户 -->
    <select id="queryUserByWhere" parameterType="User" resultType="User">
        select * from user 
        where 
        <if test="sex !=null and sex!=''">
            sex=#{sex}
        </if>
        <if test="username != null and username != ''">
            and username like "%"#{username}"%"
        </if>
    </select>


    <insert id="insertUser" parameterType="User">
        <!--<selectKey keyProperty="id" resultType="integer" order="BEFORE">-->
        <selectKey keyProperty="id" resultType="integer" order="AFTER">
            SELECT LAST_INSERT_ID()
        </selectKey>
        insert into user(username, birthday, sex, address)
        values (#{username},#{birthday},#{sex},#{address})
    </insert>

    <update id="updateUser" parameterType="int">
        update user
        set username = "updateTest"
        where id = #{id}
    </update>

    <delete id="deleteUser" parameterType="int">
        delete user from user where id = #{id}
    </delete>

    <!--================================================-->

</mapper>
