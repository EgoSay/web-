<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cjw.mapper.OrderMapper">
    <resultMap type="com.cjw.entity.Order" id="orders">
        <result column="user_id" property="userId"/>
        <result column="id" property="id"/>
        <result column="number" property="number"/>
        <result column="createtime" property="createtime"/>
        <association property="user" javaType="User">
            <id column="user_id" property="id"/>
            <result column="username" property="username"/>
        </association>
    </resultMap>
    <sql id="selector">
        select * from orders
    </sql>
    <select id="selectOrdersList" resultMap="orders">
        <include refid="selector"/>
    </select>
    <!--foreach标签使用-->
    <select id="queryOrderByIds" parameterType="com.cjw.vo.QueryVo" resultMap="orders">
        <include refid="selector"/>
        <where>
            <foreach collection="idsList" item="ids" open="id IN(" close=")" separator=",">
                    #{ids}
            </foreach>
        </where>
    </select>
    <!--一对一关联查询-->
    <select id="selectOrders" resultMap="orders">
        SELECT
	 	o.id,
	    o.user_id,
	    o.number,
	 	o.createtime,
	 	u.username
	 	FROM orders o
	 	left join user u
	 	on o.user_id = u.id
    </select>
    <!--一对多关联查询-->
    <resultMap type="User" id="user">
        <id column="user_id" property="id"/>
        <result column="username" property="username"/>
        <!-- 一对多 -->
        <collection property="orders" ofType="Orders">
            <id column="id" property="id"/>
            <result column="number" property="number"/>
        </collection>
    </resultMap>
    <select id="selectUserList" resultMap="user">
		SELECT
	 	o.id,
	    o.user_id,
	    o.number,
	 	o.createtime,
	 	u.username
	 	FROM user u
	 	left join orders o
	 	on o.user_id = u.id
	</select>
</mapper>